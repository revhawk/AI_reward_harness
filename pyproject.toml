[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-reward-harness"
version = "0.1.0"
description = "Automated Code Evaluation, AST Security Verification, and RL Scalar Reward Engine"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [
    { name = "revhawk" }
]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
